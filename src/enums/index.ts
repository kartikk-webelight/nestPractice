export enum UserRole {
  ADMIN = "admin",
  READER = "reader",
  EDITOR = "editor",
  AUTHOR = "author",
}

export enum PostStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
}

export enum CommentStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}
